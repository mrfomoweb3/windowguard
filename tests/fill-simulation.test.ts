import { describe,expect,it } from "vitest"; import { estimateBuyFill } from "../lib/fill-simulation";
describe("estimateBuyFill",()=>{it("fills across levels without mutating",()=>{const asks=[{price:.6,quantity:2},{price:.5,quantity:1}];const copy=structuredClone(asks);const x=estimateBuyFill(3,asks);expect(x.fullyFillable).toBe(true);expect(x.averagePrice).toBeCloseTo(.566666);expect(x.worstPrice).toBe(.6);expect(asks).toEqual(copy)});it("reports partial and empty books",()=>{expect(estimateBuyFill(5,[{price:.5,quantity:2}]).fillableQuantity).toBe(2);expect(estimateBuyFill(1,[]).averagePrice).toBeUndefined()});it("filters malformed and over-limit levels",()=>{const x=estimateBuyFill(2,[{price:NaN,quantity:9},{price:.5,quantity:1},{price:.7,quantity:2}],.6);expect(x.fillableQuantity).toBe(1);expect(x.levelsConsumed).toBe(1)});it("handles fractional and exact boundary",()=>{expect(estimateBuyFill(.3,[{price:.5,quantity:.3}],.5).fullyFillable).toBe(true)})});
